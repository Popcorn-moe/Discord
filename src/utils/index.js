import * as embeds from './embeds';
import { error, warn, errHandle, errorDiscord } from './log';
import load from './load';
import randomIn from './randomIn';

export { embeds, error, warn, errHandle, errorDiscord, load, randomIn };
